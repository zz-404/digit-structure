```
图的核心术语（必须掌握）

顶点（Vertex）：图中的 “节点”（比如社交网络中的人、地图中的路口）。

边（Edge）：连接两个顶点的 “关系”（比如关注、道路、通信链路）。

有向边：关系是单向的（比如 “我关注你”≠“你关注我”，对应题目中的有向图）。
无向边：关系是双向的（比如 “朋友关系”）。

权值（Weight）：边的附加信息（比如道路长度、通信延迟，对应题目中的 w）。

出度 / 入度（针对有向图）：
出度：一个顶点 “指出去” 的边的数量（比如顶点 1 有 2 条出边：1→0、1→2）。
入度：一个顶点 “接收” 的边的数量（比如顶点 0 有 2 条入边：1→0、2→0）。

稀疏图 / 稠密图：
稀疏图：边数远少于顶点数的平方（比如 100 个顶点只有 100 条边）。
稠密图：边数接近顶点数的平方（比如 100 个顶点有 9000 条边）。
```
```
构建图的方法：
可以用map直接存着
也可以构建用class那些邻接表之类的数据结构

边表：包含目的地，权值，下一条边指针

顶点表：包含顶点值和第一条边的指针

图：包含顶点数组，顶点个数，边个数
```
```
深度优先遍历dfs
核心算法：回溯
每次走到头，如果已经没有下一条路就回溯

广度优先遍历bfs
核心算法：队列
BFS 的标记时机必须是 「入队时标记」，而非「出队时」：
入队时标记，意味着 “这个顶点已经被安排处理，后续无需再入队”；
出队时标记，会导致 “入队后到出队前，其他路径可能重复入队”。
```
```
判断图是否构成树：
1. 判断边数（必须恰好是顶点-1）
2. 判断连通性
