```
void sort(int begin,int end){
    if(begin>=end){return ;}//注意大于等于，因为是整个数组的索引，i完全可能移到正整数begin的前面
    int i=begin,j=end;
    int tar=a[(begin+end)/2];
    while(i<j){
        while(a[i]<tar){i++;}
        while(a[j]>tar){j--;}
        if(i<=j){          //注意小于等于，因为等于的时候得让他们强行分开
            int temp;
            temp=a[i];a[i]=a[j];a[j]=temp;
            i++;j--;      //注意是写在括号内的,因为如果写在括号外面的话如果此时已经不满足i<=j的条件将会跳过元素，且这一步本来就是为了让等于的时候强行分开！
        }
        
    }
    sort(begin,j);
    sort(i,end);
}
```
